package hufs.likelion.gov.domain.review.repository;

import hufs.likelion.gov.domain.review.entity.MemberReviewKeyword;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MemberReviewKeywordRepository extends JpaRepository<MemberReviewKeyword, Long> {
}
