spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/gov?&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&rewriteBatchedStatements=true
    username: root
    password: tjfgmlrhks9478@
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      max-lifetime: 1770000

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
      show_sql: true
      default_batch_fetch_size: 100
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: INFO
    org.hibernate.SQL: OFF
    org.hibernate.type: OFF
    org.springframework.jdbc: OFF